---
// src/pages/index.astro
// This page will redirect users from the root URL to the English homepage.
// You could expand this to a language selection page if preferred.

import { Astro } from 'astro'; // Not strictly necessary for redirect, but good practice

// Perform the redirect
// This will instruct the browser to redirect to /en/
Astro.redirect('/en/');

// You can still include a basic HTML structure, though it won't be fully rendered
// if the redirect happens immediately. This is mostly for fallback or if JS is disabled.
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="refresh" content="0; url=/en/" /> {/* HTML Meta Refresh for immediate redirect */}
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Redirecting to Whispora.space</title>
    <style>
      body { font-family: sans-serif; display: flex; justify-content: center; align-items: center; height: 100vh; margin: 0; background-color: #f0f9ff; color: #333; }
      .message { text-align: center; padding: 20px; border-radius: 8px; background-color: #ffffff; box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); }
      a { color: #2563eb; text-decoration: none; font-weight: bold; }
      a:hover { text-decoration: underline; }
    </style>
  </head>
  <body>
    <div class="message">
      <h1>Redirecting...</h1>
      <p>If you are not redirected automatically, <a href="/en/">click here to go to the English site</a>.</p>
    </div>
  </body>
</html>
